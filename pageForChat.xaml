<Page x:Class="messangerChat.pageForChat"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:messangerChat"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="500"
      Title="pageForChat">

    <Grid Background="#483D8B">
        <Grid.RowDefinitions>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="listBox" Grid.ColumnSpan="2" Background="#483D8B"/>
        <TextBox x:Name="text" Grid.Row="1" Background="#483D8B"/>
        <Border Grid.Column="1" Grid.Row="1" Background="#9370DB" CornerRadius="20 20 20 20"/>
        <Button x:Name="button" Content="send" FontSize="26px"  Foreground="White"  Grid.Column="1" Grid.Row="1" Background="#9370DB" Margin="10 10 10 10" Click="button_Click" BorderBrush="#00707070" FontFamily="Arial Rounded MT Bold"/>

    </Grid>
</Page>
